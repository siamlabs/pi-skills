## relay

Execute a long or complex plan as a chain of independent sessions, each running
one well-sized leg and handing off to the next via `spawn_session`.

```bash
npx skills add siamlabs/pi-skills --skill relay -a pi -g
```
